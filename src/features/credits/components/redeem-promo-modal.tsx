'use client';

/**
 * @deprecated Credits system replaced by CRTVAI meToken.
 * This stub is kept for backward compatibility with the credits barrel export.
 */

interface RedeemPromoModalProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

 
export function RedeemPromoModal(props: RedeemPromoModalProps) {
  void props;
  return null;
}