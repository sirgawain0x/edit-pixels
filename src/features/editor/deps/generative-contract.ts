/**
 * Cross-feature dependency contract: generative / Evolink proxy.
 */
export {
  getPublicImageUrl,
  pollTask,
  proxyFlowRun,
  proxyGetTask,
  useGenerativeAuth,
} from '@/features/generative'
